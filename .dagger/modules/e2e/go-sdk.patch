--- a/go-sdk.dang
+++ b/go-sdk.dang
@@ -8,6 +8,9 @@
   Valid values are `default`, `empty`, and `legacy`.
   """
   template: String! = "default"
+
+  """Write selected dependency commits to the module manifests."""
+  lock: Boolean! = false

   """Find the nearest Go module root from Workspace.cwd. Return null if no go.mod exists."""
   findClientRoot(ws: Workspace!): String {
@@ -39,15 +42,9 @@
     }

     if (isModule) {
-      let manifests = generatedManifestDirectory(initialized, name, clients)
-      let configured = initialized.withDirectory(".", manifests)
-
-      # ModuleSource supplies only the built-in Go code generator. Remove
-      # its manifest output and write the configured builder output last.
-      let generated = configured
-        .withDirectory(".", generatedModuleCode(configured))
-        .withDirectory(".", manifests)
-      generateStandaloneClients(generated, clients)
+      let configured = generatedManifest(initialized, name, clients)
+      let generated = configured.withDirectory(".", generatedModuleCode(configured))
+      generateStandaloneClients(generatedManifest(generated, name, clients), clients)
     } else {
       generateStandaloneClients(initialized, clients)
     }
@@ -84,38 +81,18 @@
   }

   """Create both manifests from the scope inputs and SDK defaults."""
-  let generatedManifestDirectory(
+  let generatedManifest(
     ws: Workspace!,
     name: String!,
     clients: [ModuleSource!]!,
-  ): Directory! {
+  ): Workspace! {
     let base = sdkHelpers.moduleManifest
       .withName(name)
       .withLegacyGoRuntime

     clients.reduce(base) { manifest, target =>
-      manifest.withLegacyRuntimeDependency(
-        name: target.moduleOriginalName,
-        source: dependencySource(ws, target),
-        pin: target.pin,
-      )
-    }.directory
-  }
-
-  """Return the target address to store in both module manifests."""
-  let dependencySource(ws: Workspace!, target: ModuleSource!): String! {
-    if (target.kind == ModuleSourceKind.GIT_SOURCE) {
-      target.asString
-    } else {
-      let relative = Path(normalizePath(target.sourceRootSubpath))
-        .relativeTo(Path(normalizePath(ws.cwd)))
-      if (relative == null) {
-        raise "cannot resolve client module path " + target.sourceRootSubpath +
-          " from scope " + ws.cwd
-      } else {
-        relative.string
-      }
-    }
+      manifest.withLegacyRuntimeDependency(module: target)
+    }.generate(ws, lock: lock)
   }

   """Run built-in Go codegen and discard its manifest files."""
--- a/dagger.json
+++ b/dagger.json
@@ -7,7 +7,7 @@
   "dependencies": [
     {
       "name": "sdk-helpers",
-      "source": "github.com/dagger/sdk-helpers"
+      "source": "../sdk-helpers"
     }
   ]
 }
